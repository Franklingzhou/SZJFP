# 全量 API 测试脚本变更清单 & 手工测试介入清单

> **基准版本**：v039 (2026-06-28)  
> **对应范围**：第37轮 ~ 第41轮（v036 ~ v039）  
> **生成时间**：2026-06-28  
> **依据文档**：`docs/业务逻辑全景图.md`、`DIFFLOG.md`、`CHANGELOG.md`、线上测试结果 `reports/v038_v039_问题汇总报告.md`

---

## 第一部分：API 测试脚本变更清单

### 变更概览

| 类别 | 数量 | 说明 |
|------|:----:|------|
| 需重新编写的脚本 | 3 | 新增缺失测试覆盖的新模块/新接口 |
| 仅需调整配置的脚本 | 6 | 期望值修正、用例增删、权限断言适配 |
| 无需变更 | 1 | auth.test.js 100%通过，无变更项 |

---

### 一、需重新编写的脚本（3个）

#### 1.1 新增：培训模块测试套件 `training.test.js`

**原因**：v033 批量新增 4 个培训接口（`/api/training`、`/api/schedules`、`/api/timetables`、`/api/training-leads`），当前**零测试覆盖**。

| 新增接口 | 方法 | 全景图对应 | 测试用例设计要点 |
|----------|:----:|-----------|------------------|
| `/api/training` | GET | §11.2 培训总览仪表盘 | 6 维统计聚合验证（课程/报名/排课/证书/合同/场地），权限 admin/主管/讲师/招生 |
| `/api/schedules` | GET/POST/PUT | §11.2 排课管理 | 课节 CRUD，日期范围筛选，讲师/场地参数 |
| `/api/timetables` | GET | §11.2 课表管理 | 周/月/日视图，日期 key 生成验证 |
| `/api/training-leads` | GET/POST | §11.2 培训线索 | want_training 筛选，lead_id 转化防撞单 |

**需覆盖的 5 大类**：正向功能、参数异常、权限校验（4 类角色）、边界值、重复操作  
**预估用例数**：约 25-35 条

**权限矩阵（来自 auth-middleware.ts）**：
| 权限 Key | 允许角色 | 测试需验证 |
|----------|---------|-----------|
| `training:read` | admin, training_supervisor, instructor, recruiter | worker/customer 应 403 |
| `schedules:read` | admin, instructor, training_supervisor, recruiter, worker | customer 应 403 |
| `schedules:write` | admin, instructor, training_supervisor | recruiter/worker 应 403 |
| `timetables:read` | admin, instructor, training_supervisor, recruiter, worker | customer 应 403 |
| `training_leads:read` | admin, recruiter, training_supervisor | instructor/worker 应 403 |
| `training_leads:write` | admin, recruiter, training_supervisor | instructor/worker 应 403 |

---

#### 1.2 新增：佣金/分账/保证金/积分模块测试套件 `finance.test.js`

**原因**：全景图 §7（佣金分账体系）、§10（诚信分与保证金）及 ROADMAP 3.1 占位功能当前**零测试覆盖**。以下接口已实现但无自动化测试：

| 已有接口 | 方法 | 全景图对应 | 权限 | 风险 |
|----------|:----:|-----------|------|------|
| `/api/commission` | GET/PUT | §7.1 三级分账配置 | commission:read/write | 🔴 金额计算错误直接导致财务损失 |
| `/api/commission/settle` | POST | §7.1 手动结算 | commission:settle | 🔴 分账逻辑遗漏 |
| `/api/commission-records` | GET | §7.1 佣金记录 | commission-records:read | 🟡 |
| `/api/commission-settlements` | GET/POST | §7.1 分账结算 | commission-settlements:read/write | 🟡 |
| `/api/settlement` | GET | §7.1 分账管理 | settlement:read | 🟡 |
| `/api/credit-check` | GET | §10 诚信查询 | credit:read | 🟡 |
| `/api/credit-records` | GET | §10 诚信记录 | credit:read | 🟡 |
| `/api/credit-rules` | GET/PUT | §10 诚信规则 | credit:write | 🟡 |
| `/api/deposits` | GET/POST | §10 保证金 | deposit:read/write | 🟡 |
| `/api/refunds` | GET/POST | §8 退款 | refunds:read/write | 🟡 |
| `/api/refunds/[id]/approve` | POST | §8 退款审核 | refunds:approve | 🔴 |
| `/api/platform-fees` | GET/POST | §7.4 平台收费 | platform_fees:read/write | 🔴 双 20% 计算 |
| `/api/platform-fees/[id]` | GET/PUT | §7.4 单条编辑 | platform_fees:write | 🟡 |
| `/api/platform-fees/[id]/confirm` | POST | §7.4 确认到账 | platform_fees:write | 🔴 |
| `/api/point-records` | GET | §10 积分记录 | points:read | 🟡 |
| `/api/referral` | GET/POST | ROADMAP 转介绍 | — | 🟢 |
| `/api/referral-rewards` | GET | ROADMAP 推荐奖励 | — | 🟢 |

**预估用例数**：约 40-55 条（按每个接口 2-4 条正反向用例）

---

#### 1.3 新增：通知/操作日志/搜索/个人设置等杂项测试套件 `misc.test.js`

**原因**：以下已实现的辅助功能接口当前**零或极少量测试覆盖**：

| 接口 | 方法 | 全景图对应 | 风险 |
|------|:----:|-----------|:----:|
| `/api/notifications` | GET/POST | §12 消息通知 | 🟡 关键业务流程通知 |
| `/api/search` | GET | — 全局搜索 | 🟡 超长截断已修复但未测试 |
| `/api/profile` | GET/PUT | — 个人资料 | 🟡 8 角色 profile:write |
| `/api/levels` | GET/POST | — 等级管理 | 🟢 |
| `/api/grading` | GET/POST | — 考核评分 | 🟡 grading:write 权限隔离 |
| `/api/venues` | GET/POST | §11.1 场地管理 | 🟢 |
| `/api/operation-logs` | GET | — 操作日志 | 🟡 |
| `/api/id-card-verify` | POST | — 身份证验证 | 🟡 当前占位实现 |
| `/api/field-permissions` | GET/PUT | — 页面权限配置 | 🟡 A-29 功能 |
| `/api/dashboard` | GET | — 数据看板 | 🟢 |
| `/api/worker-tiers` | GET/POST | — 阿姨等级 | 🟢 |
| `/api/worker-applications` | GET/POST | — 阿姨自荐 | 🟡 E10 流程依赖 |
| `/api/order-signings` | GET/POST | — 订单签约 | 🔴 E03 流程依赖 |

**预估用例数**：约 30-45 条

---

### 二、仅需调整配置的脚本（6个）

#### 2.1 `create.test.js` — 期望值更新 + 用例移除 + 新增用例

| 修改类型 | 用例 ID | 具体修改项 | 原因 |
|----------|:-------:|-----------|------|
| **期望值修正** | C07-权限 | `expect.status: 401` → `403` | v039 `requirePermission()` 正确返回 403（已登录 worker 无权创建课程） |
| **期望值修正** | C16-权限 | `expect.status: 401` → `403` | 同上（已登录 customer 无权添加跟进） |
| **删除用例** | C15 (证书) | 移除整个 C15 测试块 | v038 删除独立 `/api/certificates` API（证书归入 workers.certificates JSONB） |
| **新增用例** | C04-workers | 新增「证书字段包含 certificates」正向量测试 | v038 证书存入 workers.certificates JSONB，需验证 workers POST 携带 certificates 字段 |
| **预设匹配** | 全部权限用例 | 已认证角色的权限拒绝期望值统一用 `403`，未认证用 `401` | `requirePermission()` 模式固化 |

#### 2.2 `read.test.js` — 期望值更新 + 用例移除

| 修改类型 | 用例 ID | 具体修改项 | 原因 |
|----------|:-------:|-----------|------|
| **期望值修正** | R12-权限 | `expect.status: 401` → `403` | agent 已认证但无 settings:read 权限 → 应 403 |
| **期望值修正** | R15-权限 | `expect.status: 401` → `403` | customer 已认证但无 leads:followups 读取权限 → 应 403 |
| **期望值修正** | R17-权限 | `expect.status: 401` → `403` | agent 已认证但无 resume-reviews:read → 应 403 |
| **删除用例** | R14 (证书) | 移除整个 R14 测试块 | v038 删除 `/api/certificates` API |
| **新增用例** | R01-workers | 新增「GET workers 返回 certificates 字段」验证 | v038 workers GET 需 `select` 含 certificates |
| **新增用例** | R01-workers | 新增「公开简历 GET workers/[id] 返回 certificates」 | v038 简历公开页需展示证书 |

#### 2.3 `update.test.js` — 期望值更新（最多变更项）

| 修改类型 | 用例 ID | 具体修改项 | 原因 |
|----------|:-------:|-----------|------|
| **期望值修正** | U03-权限 | `expect.status: 401` → `403` | 已登录 worker 无 customers:write → 应 403 |
| **期望值修正** | U05-权限 | `expect.status: 401` → `403` | 已登录 customer 无 courses:write → 应 403 |
| **期望值修正** | U13-权限 | `expect.status: 401` → `403` | agent 已认证但无 resume-reviews:write → 应 403 |
| **期望值修正** | U14-权限 | `expect.status: 401` → `403` | customer 已认证但无 workers:status → 应 403 |
| **期望值修正** | U15-权限(course) | `expect.status: 401` → `403` | customer 已认证但无 courses:approve → 应 403 |
| **期望值修正** | U15-权限(grade) | `expect.status: 401` → `403` | worker 已认证但无 enrollments:grade → 应 403 |
| **新增用例** | U01-workers | workers PUT 新增「含 certificates 字段更新」验证 | v038 证书 JSONB 更新通路 |
| **新增用例** | U06-reviews | reviews PATCH 新增「hide/unhide 操作」验证 | v038 新增 reviews:hide 权限 + 四种操作 |
| **新增用例** | U06-reviews | reviews PUT 新增「非 admin 修改 hidden 字段被拒绝」 | v038 评价审核隔离 |

#### 2.4 `delete.test.js` — 期望值更新 + 新增用例

| 修改类型 | 用例 ID | 具体修改项 | 原因 |
|----------|:-------:|-----------|------|
| **期望值修正** | D03-权限 | `expect.status: 401` → `403` | 已认证但无 course-package-items 写权限 → 应 403 |
| **期望值修正** | D04-权限 | `expect.status: 401` → `403` | 已认证但无 contract-templates:write → 应 403 |
| **期望值修正** | D05-权限 | `expect.status: 401` → `403` | 已认证但无 field-permissions:write → 应 403 |
| **新增用例** | D06-order-signings | admin DELETE `/api/order-signings` 清理签约记录 | v037 新增 DELETE handler（E99 测试清理依赖） |
| **新增用例** | D07-worker-applications | admin DELETE `/api/worker-applications` 清理自荐记录 | v037 新增 DELETE handler（E99 测试清理依赖） |

#### 2.5 `e2e.test.js` — 期望值更新 + 流程修正 + 新增流程

| 修改类型 | 用例 ID | 具体修改项 | 原因 |
|----------|:-------:|-----------|------|
| **期望值修正** | E08-隔离1 | `expect.status: 401` → `403` | worker 已认证但无 customers 删除权限 → 应 403 |
| **期望值修正** | E08-隔离2 | `expect.status: 401` → `403` | agent 已认证但无 field-permissions 写权限 → 应 403 |
| **流程修正** | E05 | 培训→考核打分期望调整 | v038 讲师打分不再自动发证，仅返回 excellent/qualified/failed |
| **新增流程** | E11 | 评价审核全流程 | 提交评价(pending)→管理员审核通过→验证 hidden=false→管理员隐藏→验证 hidden=true（§9.1 评价生命周期） |
| **新增流程** | E12 | 证书上传审核流程 | 上传证书(pending)→管理员审核 approved→验证 workers.certificates 状态（§2.3 证书上传权限） |

#### 2.6 `n-series.test.js` — 期望值更新 + 新增用例

| 修改类型 | 用例 ID | 具体修改项 | 原因 |
|----------|:-------:|-----------|------|
| **期望值修正** | N04-E1 | `expect.status: 401` → `403` | agent 已认证但无 resume-reviews:write → 应 403 |
| **新增用例** | N07 | 证书在简历审核中的流转 | 上传证书 → 生成 pending 审核 → admin 审核通过 → 验证 worker 中 certificates[].status=approved |
| **新增用例** | N08 | 暂停/恢复接单走简历审核 | worker 申请暂停 → 验证生成 pending resume_review → admin 审批 → 验证 worker.work_status 变更 |

---

### 三、run-all.js / _postfix_selfcheck.js 配置调整

| 文件 | 修改项 | 说明 |
|------|--------|------|
| `run-all.js` | SUITE_MAP 新增 `training`、`finance`、`misc` 3 个套件 | 对应上文新建的 3 个测试套件 |
| `_postfix_selfcheck.js` | SUITE_MAP 同步新增 3 个套件；BUG_SUITE_MAP 新增映射 | 部署门禁需覆盖新套件 |

---

### 四、helpers.js 配置调整

| 修改项 | 说明 |
|--------|------|
| `loginAs()` 登录端点 | 当前使用 `/api/auth/password-login`，确认是否需要同时支持 `phone-login`（验证码登录） |
| `fetchTestIds()` | 确认新模块（training/schedules/finance）的测试 ID 预取逻辑是否需要扩展 |

---

### 变更汇总表

| 脚本 | 需重写 | 期望值修正 | 用例删除 | 用例新增 | 变更行估计 |
|------|:---:|:---:|:---:|:---:|:---:|
| `training.test.js` | ✅ 新建 | — | — | ~25-35 | +350 |
| `finance.test.js` | ✅ 新建 | — | — | ~40-55 | +500 |
| `misc.test.js` | ✅ 新建 | — | — | ~30-45 | +400 |
| `create.test.js` | — | 2 | 1 (C15) | 1 | ±15 |
| `read.test.js` | — | 3 | 1 (R14) | 2 | ±20 |
| `update.test.js` | — | 6 | 0 | 3 | ±30 |
| `delete.test.js` | — | 3 | 0 | 2 | ±20 |
| `e2e.test.js` | — | 2 | 0 | 2 | ±40 |
| `n-series.test.js` | — | 1 | 0 | 2 | ±25 |
| `run-all.js` | — | 0 | 0 | 3 套件注册 | +6 |
| `_postfix_selfcheck.js` | — | 0 | 0 | 3 套件注册 | +6 |
| **合计** | **3 新建** | **17** | **2** | **+91-140** | **~1412** |

---

## 第二部分：手工测试介入清单

以下功能点因环境限制、权限管控、第三方依赖缺失或流程特殊性，**无法通过自动化 API 测试覆盖**，需人工介入验证。

### 类别 A：第三方依赖缺失（4 项）

| # | 功能点 | 全景图对应 | 无法自动化的原因 | 手工介入步骤 |
|:--:|--------|-----------|------------------|-------------|
| A1 | **阿里云短信验证码发送** | 注册/登录验证码 | `SMS_PROVIDER=aliyun` 需真实阿里云账号、签名模板、余额，开发模式写死 888888 | 在已配置 SMS 的生产/预发布环境手动触发注册→验证码发送→输入验证码→注册成功，验证真实短信到达率和时效 |
| A2 | **微信小程序登录** | §3 阿姨端/客户端 | `wx.login()` 获取 code 需微信开发者工具真机调试或小程序环境，无法在 Node.js 脚本中模拟 | 微信开发者工具中完成：启动小程序→自动 wx.login→调用 `/api/auth/wechat-login`→验证 openid 绑定→token 持久化→二次启动自动登录 |
| A3 | **微信支付 / 分账** | ROADMAP §13 3.0-P2 | 微信支付需真实商户号、API 证书、回调域名配置 | 在微信支付沙箱环境：创建支付订单→模拟支付回调→验证佣金分账计算→验证分账结果记录 |
| A4 | **文件上传（云存储）** | 证书照片上传、阿姨头像 | 需真实 CloudBase 存储桶 + 文件二进制流上传，API 测试仅能测 401/403 权限 | 浏览器端：上传证书照片（jpg/png）→验证返回 fileID→验证图片可通过临时 URL 访问→验证删除文件 |

### 类别 B：浏览器 UI 交互（7 项）

| # | 功能点 | 全景图对应 | 无法自动化的原因 | 手工介入步骤 |
|:--:|--------|-----------|------------------|-------------|
| B1 | **PC 端登录页** | `/admin/login` | 涉及验证码发送按钮倒计时、手机号+密码双模式切换、表单验证实时提示等 UI 状态 | 打开 `/admin/login` → 切换验证码/密码模式 → 输入手机号→点击发送验证码（验证按钮禁用+倒计时）→输入错误密码（验证错误提示）→正确登录（验证跳转） |
| B2 | **小程序端登录/注册页** | `/m/login` | 角色选择器 UI、注册表单多步骤、微信手机号授权按钮 | 移动端打开 `/m/login` → 切换到注册 Tab → 选择角色（验证隐藏 customer/worker_operator）→ 输入信息注册 → 切换到登录 Tab → 手机号+验证码登录 |
| B3 | **简历审核详情页 diff 对比** | §4 流程4 简历审核 | diff 对比表的字段高亮、变更前后值渲染是否正确，需视觉验证 | 进入 `/admin/audits/[id]` → 验证"本次修改内容"黄色区域 → 对比各字段 old/new 值差异是否正确 → 点击通过/拒绝 → 验证弹窗和结果反馈 |
| B4 | **评价审核管理页** | §9.1 评价生命周期 | v038 新增的隐藏/取消隐藏 UI 交互按钮状态切换 | 进入 `/admin/reviews` → 查看 pending 评价 → 点击审核通过 → 验证评价变为可见 → 点击隐藏 → 验证评价变为灰色隐藏态 → 点击取消隐藏 |
| B5 | **证书管理页** | §2.3 证书上传权限 | v038 重写的 `/admin/certificates` 页面从 workers.certificates JSONB 读数据，需验证卡片渲染、审核按钮 | 进入 `/admin/certificates` → 验证待审核证书卡片展示（name/authority/date/image）→ 点击通过 → 验证 status 变化 → 点击拒绝 → 验证拒绝状态 |
| B6 | **订单签约→阿姨确认弹窗** | §4 流程2 经纪人服务线 | 签约弹窗涉及推荐阿姨选择、阿姨手机确认码输入，多步骤 UI 交互 | PC端：经纪人创建订单→选择推荐阿姨→发起签约→阿姨端收到确认→输入手机确认码→验证签约完成 |
| B7 | **客户流失级联确认对话框** | §3.4 客户状态 | `/api/orders/[id]/lost` 调用后需在前端验证客户列表状态变化+提示弹窗 | 经纪人端：进入客户管理→选择客户→点击「标记流失」→确认弹窗→验证客户状态变为 closed→验证关联订单变 cancelled |

### 类别 C：权限 UI 表现（5 项）

| # | 功能点 | 全景图对应 | 无法自动化的原因 | 手工介入步骤 |
|:--:|--------|-----------|------------------|-------------|
| C1 | **侧边栏菜单按角色过滤** | §2 8 种角色体系 | 不同角色登录后侧边栏菜单项不同，需分别登录验证 | 分别用 agent/recruiter/instructor/training_supervisor/worker_operator 登录 PC 端 → 验证侧边栏菜单项是否符合全景图 §2 权限定义 → 验证不可见菜单直接访问 URL 是否被拒绝 |
| C2 | **tab-bar 按角色过滤** | §2 双平台架构 | 小程序端不同角色底部导航栏不同 | 分别用 worker/customer 登录移动端 → 验证 tab-bar 显示的 Tab 数量和文字 → 验证点击每个 Tab 正常跳转 |
| C3 | **订单大厅角色隔离** | §6 规则2 | 不同角色看订单大厅的可见范围不同（agent 看全部、customer 只看已签约） | agent 登录查看大厅 → 验证可看到所有订单 → customer 登录 → 验证仅显示已签约订单 → worker 登录 → 验证可看到 open 状态订单用于投递 |
| C4 | **数据导出按钮权限** | §4 流程4 | `/api/workers/export` 仅 admin 有权限，需验证按钮是否仅在 admin 界面显示 | admin 登录 → 阿姨管理页 → 验证「导出」按钮可见 → 点击导出 CSV → 验证文件下载 → agent 登录 → 验证导出按钮不显示 |
| C5 | **页面权限配置 (A-29)** | `/api/field-permissions` | 管理员配置角色可见菜单后，对应角色登录需验证菜单是否实时生效 | 管理员进入系统设置→页面权限配置→切换某角色可见菜单→保存→用该角色重新登录→验证菜单变化 |

### 类别 D：端到端长流程（5 项）

| # | 功能点 | 全景图对应 | 无法自动化的原因 | 手工介入步骤 |
|:--:|--------|-----------|------------------|-------------|
| D1 | **完整招生培训线** | §4 流程1 | 跨 7 个步骤（录入线索→跟进→签约→报名→排课→考核打分→转简历），含多角色切换 | 招生录入线索→添加跟进记录→签培训合同→主管代确认→报名课程→排课→讲师考核打分 excellent/qualified/failed→验证考核结果→转简历→管理员审核→验证阿姨进大厅 |
| D2 | **完整经纪人服务线** | §4 流程2 | 跨 6 个步骤（录入客户→发单→推荐→签约→上户→评价），涉及阿姨手机确认 | 经纪人录入客户→发单(open)→推荐阿姨→签约(阿姨手机确认)→上户确认(POST start)→上户完成→客户评价→管理员审核评价上线 |
| D3 | **更换阿姨流程** | §6 规则7 | 同一订单内完成更换，需验证旧阿姨释放+新阿姨签约+合同期不变 | 已签约订单→经纪人发起更换阿姨→选择新推荐阿姨→签约→验证旧阿姨 work_status→available、新阿姨→working→验证合同时间不变 |
| D4 | **客户流失全链路** | §6 规则9 + §3.4 | 流失触发 4 个级联操作，需人工验证全链路 | 经纪人标记客户流失→验证订单→cancelled→客户→closed→推荐→rejected→阿姨→available→平台费→overdue |
| D5 | **合同到期手动关闭+级联** | §8 合同体系 | v034 新增 closed 状态流转 + 平台费 overdue 级联 | 管理员进入合同管理→选择 active 合同→点击手动关闭→验证状态变 closed→验证 closed_at/closed_by 记录→验证关联待付平台费变 overdue |

### 类别 E：真实环境数据校验（3 项）

| # | 功能点 | 全景图对应 | 无法自动化的原因 | 手工介入步骤 |
|:--:|--------|-----------|------------------|-------------|
| E1 | **种子数据完整性** | AGENTS.md 数据库表清单 | 15 张表的种子数据是否完整、关联外键是否正确，需人工 SQL 查证 | 在 Supabase SQL Editor 中逐表 `SELECT count(*)` 验证记录数 → 验证 users 表 8 种角色各有至少 1 条 → 验证 workers/orders/reviews 等关联表外键有效 |
| E2 | **RLS 策略验证** | Supabase Row Level Security | RLS 策略是否阻止跨用户数据访问，需用不同 anon key 模拟 | 用不同用户 token 直接调用 Supabase REST API → 验证是否只能读取自己有权限的数据 → 验证 update/delete 是否被 RLS 拒绝 |
| E3 | **cron 定时任务** | §11.3 培训周期定时任务 | 定时任务触发时机依赖系统时间，API 测试无法模拟时间流逝 | 将某 enrollment 的 created_at 改为 8 天前 → 手动触发 cron → 验证通知是否生成 → 将合同 end_date 改为昨天 → 验证合同到期提醒通知 |

---

## 第三部分：优先级建议

### 🔴 高优先级（影响功能验证和部署安全）

| 序号 | 事项 | 类型 | 预计工时 |
|:----:|------|------|:------:|
| 1 | 17 条期望值 401→403 修正（6 个测试脚本） | 配置调整 | 2h |
| 2 | `e2e.test.js` 新增评价审核全流程（E11） | 用例新增 | 1.5h |
| 3 | `e2e.test.js` 新增证书上传审核流程（E12） | 用例新增 | 1h |
| 4 | `create.test.js` 删除 C15 / `read.test.js` 删除 R14 | 用例清理 | 0.5h |
| 5 | 手工验证 D1（完整招生培训线） | 手工测试 | 1h |
| 6 | 手工验证 D2（完整经纪人服务线） | 手工测试 | 1h |

### 🟡 中优先级（补测试覆盖盲区）

| 序号 | 事项 | 类型 | 预计工时 |
|:----:|------|------|:------:|
| 7 | 新建 `training.test.js` | 新建脚本 | 4h |
| 8 | 新建 `finance.test.js` | 新建脚本 | 5h |
| 9 | 手工验证 A1~A4（第三方依赖） | 手工测试 | 2h |
| 10 | 手工验证 C1~C5（权限 UI 表现） | 手工测试 | 1.5h |
| 11 | 手工验证 D3~D5（长流程） | 手工测试 | 1.5h |

### 🟢 低优先级（锦上添花）

| 序号 | 事项 | 类型 | 预计工时 |
|:----:|------|------|:------:|
| 12 | 新建 `misc.test.js` | 新建脚本 | 4h |
| 13 | 手工验证 B1~B7（浏览器 UI） | 手工测试 | 2h |
| 14 | 手工验证 E1~E3（真实环境） | 手工测试 | 1.5h |
| 15 | `run-all.js` / `_postfix_selfcheck.js` 注册新套件 | 配置调整 | 0.5h |

---

## 第四部分：附：v038/v039 失败用例→期望值修正速查表

以下 17 条为确定性修改（不改会导致持续误报），可直接批量操作：

| # | 文件 | 用例标签关键词 | 修改：`expect.status` |
|:--:|------|---------------|:-------------------:|
| 1 | `create.test.js` | `C07-权限-阿姨创建课程` | `401` → `403` |
| 2 | `create.test.js` | `C16-权限-客户添加跟进`（若有） | `401` → `403` |
| 3 | `read.test.js` | `R12-权限-经纪人获取系统设置` | `401` → `403` |
| 4 | `read.test.js` | `R15-权限-客户看跟进记录` | `401` → `403` |
| 5 | `read.test.js` | `R17-权限-经纪人查看审核列表` | `401` → `403` |
| 6 | `update.test.js` | `U03-权限-阿姨更新客户` | `401` → `403` |
| 7 | `update.test.js` | `U05-权限-客户更新课程` | `401` → `403` |
| 8 | `update.test.js` | `U13-权限-经纪人审核简历` | `401` → `403` |
| 9 | `update.test.js` | `U14-权限-客户暂停阿姨` | `401` → `403` |
| 10 | `update.test.js` | `U15-权限-客户审核课程` | `401` → `403` |
| 11 | `update.test.js` | `U15-权限-学员给自己打分` | `401` → `403` |
| 12 | `delete.test.js` | `D03-权限-非授权删除套餐项目` | `401` → `403` |
| 13 | `delete.test.js` | `D04-权限-非授权停用合同模板` | `401` → `403` |
| 14 | `delete.test.js` | `D05-权限-非授权删除字段权限配置` | `401` → `403` |
| 15 | `e2e.test.js` | `E08-隔离-worker无法删除客户` | `401` → `403` |
| 16 | `e2e.test.js` | `E08-隔离-agent无法管理字段权限` | `401` → `403` |
| 17 | `n-series.test.js` | `N04-E1-经纪人审核简历` | `401` → `403` |

---

> **报告结束**  
> 生成工具：AI 代码分析（基于 DIFFLOG 第37~41轮 + 业务逻辑全景图 + 线上 v039 自检结果）  
> 下一步：按优先级顺序逐项执行，每完成一项更新此清单状态。
