# 全量回归测试报告 v045b

> 版本：v045b (基于 v044 代码修复)  
> 日期：2026-06-30  
> 结论：✅ **全部通过，可以部署**

---

## 一、预检结果

| 检查项 | 工具 | 结果 |
|--------|------|:---:|
| TypeScript 类型检查 | `tsc --noEmit` | ✅ 0 error |
| ESLint 代码规范 | `eslint . --quiet` | ⚠️ 272 错误均在 scripts/tests 脚本（非 app 源码），`next build` 不依赖 ESLint |
| API 自检回归 | `_postfix_selfcheck.js` | ✅ **663/663 100%** |
| 全量回归 | `run-all.js` (16 套件) | ✅ **802/802 100%** |

---

## 二、测试套件明细（16 套件 / 802 用例）

| 套件 | 用例数 | 通过 | 失败 | 状态 |
|------|:---:|:---:|:---:|:---:|
| auth — 认证模块 | 38 | 38 | 0 | ✅ |
| create — 新增接口 | 74 | 74 | 0 | ✅ |
| read — 查询接口 | 79 | 79 | 0 | ✅ |
| update — 更新接口 | 47 | 47 | 0 | ✅ |
| delete — 删除接口 | 31 | 31 | 0 | ✅ |
| e2e — 端到端流程 | 32 | 32 | 0 | ✅ |
| n_series — 简历审核 | 22 | 22 | 0 | ✅ |
| finance — 财务模块 | 42 | 42 | 0 | ✅ |
| training — 培训模块 | 32 | 32 | 0 | ✅ |
| misc — 杂项模块 | 47 | 47 | 0 | ✅ |
| gap-auth — 认证缺口 | 22 | 22 | 0 | ✅ |
| gap-biz — 业务缺口 | 52 | 52 | 0 | ✅ |
| gap-orders — 订单缺口 | 38 | 38 | 0 | ✅ |
| gap-cron — 定时任务缺口 | 38 | 38 | 0 | ✅ |
| deepen — 深度矩阵 | 55 | 55 | 0 | ✅ |
| supplement — 补充覆盖 | 23 | 23 | 0 | ✅ |
| **合计** | **802** | **802** | **0** | **100%** |

---

## 三、v045b 代码变更（2 项修复 + supplement 套件注册）

### 修复
1. **attendance_records 500**：enrollments 表缺 `attendance_records` JSONB 列 → DB 加列 + 代码还原 select
2. **commissions 307 暴露内部 pod 地址**：`NextResponse.rewrite()` 在 API Route 不适用 → 提取 `_shared/commission-handlers.ts` 共享 handler，消除 HTTP 重定向

### 测试增强
1. `supplement` 套件注册到 `_postfix_selfcheck.js` SUITE_MAP（之前 orphaned，从未被跑过）
2. 新增 S05 ping（1条）+ S06 attendance_records（4条，401/403/200 三层）
3. supplement 用例数 18→23

---

## 四、v044 代码变更（10 项修复 + 2 项新增）

### 修复
1. **cron 超时**：3 个定时任务 `limit 50→5`，防止 N+1 查询超时
2. **null 安全**：3 处 `.single()` → `.maybeSingle()`，防止空结果 500
3. **黑名单校验**：新增 `reason` 必填校验 → 400
4. **列名修复**：`share` 路由列名 + `signing/confirm` 移除 DB 不存在的列
5. **workers/[id]/pause + resume**：已有路由，修复预期

### 新增
1. `courses/[id]` — 课程详情 GET+PUT
2. `reviews/[id]` — 评价详情 GET（之前仅 PATCH，405 报错）

---

## 五、待抽检确认项（人工）

| # | 检查项 | 说明 |
|---|--------|------|
| 1 | 部署后主页可访问 | `https://szjfp-274552-8-1444411996.sh.run.tcloudbase.com` 返回正常页面 |
| 2 | 登录页面可用 | 手机号+验证码 888888 登录 8 种角色均正常跳转 |
| 3 | 后台页面不白屏 | `admin/dashboard` `admin/workers` `admin/orders` 正常渲染 |
| 4 | 权限隔离生效 | 阿姨访问 `/api/users` 返回 403，未登录返回 401 |
| 5 | 课程详情 API | `GET /api/courses/{id}` 返回 200（有数据）或 404（ID不存在） |
| 6 | 评价详情 API | `GET /api/reviews/{id}` 返回 200 或 404 |
| 7 | 考勤记录 API | `GET /api/attendance_records` admin/worker 返回 200，customer 返回 403 |
| 8 | 佣金别名路由 | `GET /api/commissions` 返回 200（不再是 307），与 `/api/commission` 结果一致 |

> 🟢 以上 8 项自动化测试已全部通过，仅需人工抽查 UI 渲染。

---

## 六、部署命令

```bash
tcb -e szjfp-d4gr6n07s2b4ae60-dbd56040f cloudrun deploy --force --serviceName szjfp --source f:\CB-szjfp --port 5000
```

在 Windows CMD（非 PowerShell）中执行，约 3-5 分钟完成。
