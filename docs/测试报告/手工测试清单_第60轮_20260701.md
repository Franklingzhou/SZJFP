                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    # 手工测试清单 — 第60轮 (2026-07-01)

> 📋 **配套完整清单**: [上线前完整测试清单 v2.3 (189项)](./上线前完整测试清单_v2.3_20260701.md) — 覆盖冒烟/功能/权限/数据/兼容/性能/异常/安全/业务链路/运维/回归 11 大维度，含 4 个 P0 上线阻塞安全漏洞。

## 概述
本轮修改覆盖 **17 个文件**，涉及小程序端 7 角色 + PC 管理后台 3 个页面 + 上传功能。需逐页浏览器验证。

---

## 一、小程序端 (m/) — 第59轮修复

### 1. 阿姨运营 — `/m/worker_operator`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 首页加载 | 以 worker_operator 角色登录，打开首页 | 页面正常显示，不 404 |
| 所有菜单点击 | 逐一点击底部导航栏每个菜单 | 不出现 404 |

### 2. 阿姨课程 — `/m/worker/courses`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 课程列表 | 以 worker 角色登录，点击「课程」tab | 能看到课程数据 |

### 3. 阿姨简历 — `/m/worker/resume` ⭐ 本周上传修复
| 检查项 | 操作 | 预期 |
|--------|------|------|
| **上传头像** | 编辑模式 → 点击头像上传区 → 选择图片 | 显示"上传中..."→头像变为上传后的 URL 图片 |
| **上传身份证人像面** | 编辑模式 → 身份证区 → 点击人像面上传 | 上传后显示真实图片，而非灰底"已上传"文字 |
| **上传身份证国徽面** | 同上，国徽面 | 同上 |
| **上传照片** | 编辑模式 → 照片相册 → 添加照片 → 选分类 → 选文件 | 上传后相册中显示真实图片 |
| **征信查询** | 点击征信查询 → 发起查询 | 失败时显示"征信查询暂时不可用"，不再显示假征信报告 |
| **简历保存** | 编辑信息 → 点保存 | 显示"简历已提交审核"toast |

### 4. 客户评价 — `/m/customer/orders`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 订单列表 | 以 customer 角色登录 → 我的订单 | 能看到订单，评价按钮可用 |
| 评价提交 | 点评价 → 打分+写内容 → 提交 | 提交成功，评价关联到对应阿姨 |

### 5. 经纪人客户管理 — `/m/agent/customers` ⭐ 新增API
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 客户列表加载 | 以 agent 登录 → 客户管理 | 能看到真实客户数据 |
| **新增客户** | 点新增 → 填写信息 → 提交 | 提交成功，列表刷新 |
| **编辑客户** | 点编辑 → 修改 → 保存 | 保存成功 |
| **添加跟进** | 点某客户 → 跟进记录 → 填写 → 提交 | 提交成功，刷新还在 |
| **一键发单** | 点一键发单 → 填写订单信息 → 提交 | 创建订单成功 |

### 6. 经纪人首页 — `/m/agent`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| **修改订单** | 点修改 → 修改字段 → 确认 | 修改后数据持久化，刷新不丢失 |

### 7. 招生代理跟进 — `/m/recruiter/follow`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 线索列表 | 以 recruiter 登录 → 跟进管理 | 能看到线索数据 |
| **写跟进** | 选一条线索 → 写跟进记录 → 提交 | 提交成功，刷新后还在 |

### 8. 培训主管线索 — `/m/training_supervisor/leads`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 线索列表 | 以 training_supervisor 登录 → 线索管理 | 能看到数据 |
| **写跟进** | 同招生代理 | 持久化保存 |

### 9. 讲师课程管理 — `/m/instructor/courses` ⭐ 新增功能
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 课程列表 | 以 instructor 登录 → 课程管理 | 能看到真实课程 |
| **考勤签到** | 点某课程 → 考勤 → 操作 | 能调起签到弹窗 |
| **考核打分** | 点打分 → 输入分数 → 提交 | 提交成功 |
| **培训点评** | 点点评 → 星级评分 → 提交 | 评价创建成功 |

### 10. 讲师学员管理 — `/m/instructor/students`
| 检查项 | 操作 | 预期 |
|--------|------|------|
| **培训点评** | 学员列表 → 点某学员的「培训点评」按钮 | 弹出星级评分弹窗 → 提交成功 |

---

## 二、PC 管理后台 (admin/) — 第60轮新增修复

### 11. 场地管理 — `/admin/venues` ⭐ 假数据→真API
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 页面加载 | 以 admin 登录 → 场地管理 | 能看到从数据库加载的真实场地数据 |
| **新增场地** | 点"添加场地" → 填名称/容量/位置 → 保存 | 显示"场地创建成功"，列表刷新 |
| 加载失败 | 断网情况打开页面 | 显示"加载场地数据失败"而不是静默假数据 |

### 12. 诚信分管理 — `/admin/credit` ⭐ 假规则→真API
| 检查项 | 操作 | 预期 |
|--------|------|------|
| 切换到规则配置 | 点「规则配置」tab | 从 API 加载规则，显示"加载规则中..."后出现规则列表 |
| API 失败回退 | 断开 API 后刷新规则 | 自动使用默认 14 条种子规则，不空白 |
| 规则编辑/保存 | 修改规则 → 点「保存规则」 | 保存到 system_settings 表 |

### 13. 侧边栏 — 全局
| 检查项 | 操作 | 预期 |
|--------|------|------|
| **死链消失** | 查看左侧导航栏 | 不再显示「我的简历」和「我的合同」两个 404 链接 |
| 其他链接 | 逐个点击导航项 | 均能正确跳转到对应页面，不 404 |

---

## 三、需浏览器自动化验证的页面（推荐跑 Playwright）

以下页面改动了交互逻辑，建议用 Playwright 跑一遍按钮点击：

| 序号 | 页面路由 | 关键操作 |
|------|---------|---------|
| 1 | `/m/worker/resume` | 上传头像/身份证/照片，保存简历 |
| 2 | `/m/agent/customers` | 新增客户 → 编辑 → 跟进 → 一键发单 |
| 3 | `/m/agent` | 修改订单弹窗 → 保存 |
| 4 | `/m/instructor/courses` | 考勤/打分/点评三个弹窗 |
| 5 | `/m/recruiter/follow` | 写跟进 → 提交 |
| 6 | `/admin/venues` | 加载列表 → 新增场地 → 预约 |
| 7 | `/admin/credit` | 规则配置 tab → 编辑规则 → 保存 |

---

## 四、已知未修复问题（遗留）

| 问题 | 位置 | 状态 |
|------|------|:---:|
| 视频上传 | `/m/worker/resume` 视频区 | 上传 API 不支持 video 类型，仍是本地 URL.createObjectURL |
| 场地删除 | `/admin/venues` 删除按钮 | DELETE API 不存在，暂提示需数据库操作 |
| PC 多处 catch 吞错误 | settlement/orders/deposit 等 | 多文件，低优先级 |
| 预约持久化 | `/admin/venues` 预约记录 | 目前仅存本地 state，刷新丢失 |

---

## 测试速查表

```
快速验证命令：
  curl http://localhost:3000/admin/venues          → 应返回 200 + 真实数据
  curl http://localhost:3000/api/credit-rules       → 应返回规则 JSON
  curl http://localhost:3000/m/worker_operator      → 应返回 200 (非 404)
```
